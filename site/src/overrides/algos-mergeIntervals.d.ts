// Type stub for @algos/mergeIntervals/mergeIntervals.
// Keeps TypeScript from type-checking the src/ file directly, which has an
// unused local `startRes` that we cannot remove (src/ is read-only).
// Vite's resolve.alias still maps @algos/* to the real src/ at runtime.
export function mergeIntervals(intervals: number[][]): number[][];
